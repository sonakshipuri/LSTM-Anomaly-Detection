# LSTM-Anomaly-Detection
LSTM Autoencoder for unsupervised anomaly detection on the SKAB industrial sensor dataset. Trained only on normal operating data and optimized through hyperparameter tuning. Achieved F1=0.792 and AUC-ROC=0.885, outperforming an Isolation Forest baseline by 16.2% in F1 score.
